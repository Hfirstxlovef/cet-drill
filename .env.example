# 复制本文件为 .env 并填入你自己的值：cp .env.example .env

# 数据库（SQLite，路径相对 prisma/schema.prisma）
DATABASE_URL="file:./dev.db"

# ZenMux API Key（或任意 OpenAI 兼容服务的 Key），也可在「设置」页面填写
ZENMUX_API_KEY=""

# 可选：自定义 API 网关地址，默认即下方地址
ZENMUX_BASE_URL="https://zenmux.ai/api/v1"
